<h1>
  <%= @cocktail.name %>
</h1>

<p>In order to prepare a great <%= @cocktail.name %> you will need:</p>

<div>
  <ul>
    <% @cocktail.doses.each do |dose| %>
      <li>
        <%= dose.ingredient.name %> | <%= dose.description %> | <%= link_to "🗑", cocktail_dose_path(@cocktail),
            method: :delete,
            data: { confirm: "Are you sure you want to delete this dose?" }%>
      </li>
    <%end%>
  </ul>
</div>


<div>
  <%= link_to "Add a new dose", new_cocktail_dose_path(@cocktail) %>
</div>

<div>
  <%= link_to "All cocktails", cocktails_path %>
</div>
